// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ntiGideon/ent/budget"
	"github.com/ntiGideon/ent/budgetline"
)

// BudgetLine is the model entity for the BudgetLine schema.
type BudgetLine struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Category holds the value of the "category" field.
	Category string `json:"category,omitempty"`
	// LineType holds the value of the "line_type" field.
	LineType budgetline.LineType `json:"line_type,omitempty"`
	// AllocatedAmount holds the value of the "allocated_amount" field.
	AllocatedAmount float64 `json:"allocated_amount,omitempty"`
	// Currency holds the value of the "currency" field.
	Currency string `json:"currency,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes string `json:"notes,omitempty"`
	// BudgetID holds the value of the "budget_id" field.
	BudgetID int `json:"budget_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the BudgetLineQuery when eager-loading is set.
	Edges        BudgetLineEdges `json:"edges"`
	selectValues sql.SelectValues
}

// BudgetLineEdges holds the relations/edges for other nodes in the graph.
type BudgetLineEdges struct {
	// Budget holds the value of the budget edge.
	Budget *Budget `json:"budget,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// BudgetOrErr returns the Budget value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e BudgetLineEdges) BudgetOrErr() (*Budget, error) {
	if e.Budget != nil {
		return e.Budget, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: budget.Label}
	}
	return nil, &NotLoadedError{edge: "budget"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BudgetLine) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case budgetline.FieldAllocatedAmount:
			values[i] = new(sql.NullFloat64)
		case budgetline.FieldID, budgetline.FieldBudgetID:
			values[i] = new(sql.NullInt64)
		case budgetline.FieldCategory, budgetline.FieldLineType, budgetline.FieldCurrency, budgetline.FieldNotes:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BudgetLine fields.
func (_m *BudgetLine) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case budgetline.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case budgetline.FieldCategory:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field category", values[i])
			} else if value.Valid {
				_m.Category = value.String
			}
		case budgetline.FieldLineType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field line_type", values[i])
			} else if value.Valid {
				_m.LineType = budgetline.LineType(value.String)
			}
		case budgetline.FieldAllocatedAmount:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field allocated_amount", values[i])
			} else if value.Valid {
				_m.AllocatedAmount = value.Float64
			}
		case budgetline.FieldCurrency:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field currency", values[i])
			} else if value.Valid {
				_m.Currency = value.String
			}
		case budgetline.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		case budgetline.FieldBudgetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field budget_id", values[i])
			} else if value.Valid {
				_m.BudgetID = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BudgetLine.
// This includes values selected through modifiers, order, etc.
func (_m *BudgetLine) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryBudget queries the "budget" edge of the BudgetLine entity.
func (_m *BudgetLine) QueryBudget() *BudgetQuery {
	return NewBudgetLineClient(_m.config).QueryBudget(_m)
}

// Update returns a builder for updating this BudgetLine.
// Note that you need to call BudgetLine.Unwrap() before calling this method if this BudgetLine
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BudgetLine) Update() *BudgetLineUpdateOne {
	return NewBudgetLineClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BudgetLine entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BudgetLine) Unwrap() *BudgetLine {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BudgetLine is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BudgetLine) String() string {
	var builder strings.Builder
	builder.WriteString("BudgetLine(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("category=")
	builder.WriteString(_m.Category)
	builder.WriteString(", ")
	builder.WriteString("line_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.LineType))
	builder.WriteString(", ")
	builder.WriteString("allocated_amount=")
	builder.WriteString(fmt.Sprintf("%v", _m.AllocatedAmount))
	builder.WriteString(", ")
	builder.WriteString("currency=")
	builder.WriteString(_m.Currency)
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteString(", ")
	builder.WriteString("budget_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.BudgetID))
	builder.WriteByte(')')
	return builder.String()
}

// BudgetLines is a parsable slice of BudgetLine.
type BudgetLines []*BudgetLine
