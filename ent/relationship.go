// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ntiGideon/ent/contact"
	"github.com/ntiGideon/ent/relationship"
)

// Relationship is the model entity for the Relationship schema.
type Relationship struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// FromContactID holds the value of the "from_contact_id" field.
	FromContactID int `json:"from_contact_id,omitempty"`
	// ToContactID holds the value of the "to_contact_id" field.
	ToContactID int `json:"to_contact_id,omitempty"`
	// RelationType holds the value of the "relation_type" field.
	RelationType relationship.RelationType `json:"relation_type,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes string `json:"notes,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RelationshipQuery when eager-loading is set.
	Edges        RelationshipEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RelationshipEdges holds the relations/edges for other nodes in the graph.
type RelationshipEdges struct {
	// FromContact holds the value of the from_contact edge.
	FromContact *Contact `json:"from_contact,omitempty"`
	// ToContact holds the value of the to_contact edge.
	ToContact *Contact `json:"to_contact,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// FromContactOrErr returns the FromContact value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RelationshipEdges) FromContactOrErr() (*Contact, error) {
	if e.FromContact != nil {
		return e.FromContact, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: contact.Label}
	}
	return nil, &NotLoadedError{edge: "from_contact"}
}

// ToContactOrErr returns the ToContact value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RelationshipEdges) ToContactOrErr() (*Contact, error) {
	if e.ToContact != nil {
		return e.ToContact, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: contact.Label}
	}
	return nil, &NotLoadedError{edge: "to_contact"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Relationship) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case relationship.FieldID, relationship.FieldFromContactID, relationship.FieldToContactID:
			values[i] = new(sql.NullInt64)
		case relationship.FieldRelationType, relationship.FieldNotes:
			values[i] = new(sql.NullString)
		case relationship.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Relationship fields.
func (_m *Relationship) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case relationship.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case relationship.FieldFromContactID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field from_contact_id", values[i])
			} else if value.Valid {
				_m.FromContactID = int(value.Int64)
			}
		case relationship.FieldToContactID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field to_contact_id", values[i])
			} else if value.Valid {
				_m.ToContactID = int(value.Int64)
			}
		case relationship.FieldRelationType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field relation_type", values[i])
			} else if value.Valid {
				_m.RelationType = relationship.RelationType(value.String)
			}
		case relationship.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		case relationship.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Relationship.
// This includes values selected through modifiers, order, etc.
func (_m *Relationship) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryFromContact queries the "from_contact" edge of the Relationship entity.
func (_m *Relationship) QueryFromContact() *ContactQuery {
	return NewRelationshipClient(_m.config).QueryFromContact(_m)
}

// QueryToContact queries the "to_contact" edge of the Relationship entity.
func (_m *Relationship) QueryToContact() *ContactQuery {
	return NewRelationshipClient(_m.config).QueryToContact(_m)
}

// Update returns a builder for updating this Relationship.
// Note that you need to call Relationship.Unwrap() before calling this method if this Relationship
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Relationship) Update() *RelationshipUpdateOne {
	return NewRelationshipClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Relationship entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Relationship) Unwrap() *Relationship {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Relationship is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Relationship) String() string {
	var builder strings.Builder
	builder.WriteString("Relationship(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("from_contact_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.FromContactID))
	builder.WriteString(", ")
	builder.WriteString("to_contact_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ToContactID))
	builder.WriteString(", ")
	builder.WriteString("relation_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.RelationType))
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Relationships is a parsable slice of Relationship.
type Relationships []*Relationship
