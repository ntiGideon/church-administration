// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ntiGideon/ent/contact"
	"github.com/ntiGideon/ent/roster"
	"github.com/ntiGideon/ent/rosterentry"
)

// RosterEntry is the model entity for the RosterEntry schema.
type RosterEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// RosterID holds the value of the "roster_id" field.
	RosterID int `json:"roster_id,omitempty"`
	// ContactID holds the value of the "contact_id" field.
	ContactID int `json:"contact_id,omitempty"`
	// Role holds the value of the "role" field.
	Role string `json:"role,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes string `json:"notes,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RosterEntryQuery when eager-loading is set.
	Edges        RosterEntryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RosterEntryEdges holds the relations/edges for other nodes in the graph.
type RosterEntryEdges struct {
	// Roster holds the value of the roster edge.
	Roster *Roster `json:"roster,omitempty"`
	// Contact holds the value of the contact edge.
	Contact *Contact `json:"contact,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// RosterOrErr returns the Roster value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RosterEntryEdges) RosterOrErr() (*Roster, error) {
	if e.Roster != nil {
		return e.Roster, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: roster.Label}
	}
	return nil, &NotLoadedError{edge: "roster"}
}

// ContactOrErr returns the Contact value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RosterEntryEdges) ContactOrErr() (*Contact, error) {
	if e.Contact != nil {
		return e.Contact, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: contact.Label}
	}
	return nil, &NotLoadedError{edge: "contact"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RosterEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case rosterentry.FieldID, rosterentry.FieldRosterID, rosterentry.FieldContactID:
			values[i] = new(sql.NullInt64)
		case rosterentry.FieldRole, rosterentry.FieldNotes:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RosterEntry fields.
func (_m *RosterEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case rosterentry.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case rosterentry.FieldRosterID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field roster_id", values[i])
			} else if value.Valid {
				_m.RosterID = int(value.Int64)
			}
		case rosterentry.FieldContactID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field contact_id", values[i])
			} else if value.Valid {
				_m.ContactID = int(value.Int64)
			}
		case rosterentry.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				_m.Role = value.String
			}
		case rosterentry.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RosterEntry.
// This includes values selected through modifiers, order, etc.
func (_m *RosterEntry) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRoster queries the "roster" edge of the RosterEntry entity.
func (_m *RosterEntry) QueryRoster() *RosterQuery {
	return NewRosterEntryClient(_m.config).QueryRoster(_m)
}

// QueryContact queries the "contact" edge of the RosterEntry entity.
func (_m *RosterEntry) QueryContact() *ContactQuery {
	return NewRosterEntryClient(_m.config).QueryContact(_m)
}

// Update returns a builder for updating this RosterEntry.
// Note that you need to call RosterEntry.Unwrap() before calling this method if this RosterEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RosterEntry) Update() *RosterEntryUpdateOne {
	return NewRosterEntryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RosterEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RosterEntry) Unwrap() *RosterEntry {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RosterEntry is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RosterEntry) String() string {
	var builder strings.Builder
	builder.WriteString("RosterEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("roster_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RosterID))
	builder.WriteString(", ")
	builder.WriteString("contact_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ContactID))
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(_m.Role)
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteByte(')')
	return builder.String()
}

// RosterEntries is a parsable slice of RosterEntry.
type RosterEntries []*RosterEntry
