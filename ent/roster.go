// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ntiGideon/ent/church"
	"github.com/ntiGideon/ent/roster"
)

// Roster is the model entity for the Roster schema.
type Roster struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// ServiceDate holds the value of the "service_date" field.
	ServiceDate time.Time `json:"service_date,omitempty"`
	// Department holds the value of the "department" field.
	Department string `json:"department,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes string `json:"notes,omitempty"`
	// ChurchID holds the value of the "church_id" field.
	ChurchID int `json:"church_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RosterQuery when eager-loading is set.
	Edges        RosterEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RosterEdges holds the relations/edges for other nodes in the graph.
type RosterEdges struct {
	// Church holds the value of the church edge.
	Church *Church `json:"church,omitempty"`
	// Entries holds the value of the entries edge.
	Entries []*RosterEntry `json:"entries,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ChurchOrErr returns the Church value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RosterEdges) ChurchOrErr() (*Church, error) {
	if e.Church != nil {
		return e.Church, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: church.Label}
	}
	return nil, &NotLoadedError{edge: "church"}
}

// EntriesOrErr returns the Entries value or an error if the edge
// was not loaded in eager-loading.
func (e RosterEdges) EntriesOrErr() ([]*RosterEntry, error) {
	if e.loadedTypes[1] {
		return e.Entries, nil
	}
	return nil, &NotLoadedError{edge: "entries"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Roster) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case roster.FieldID, roster.FieldChurchID:
			values[i] = new(sql.NullInt64)
		case roster.FieldTitle, roster.FieldDepartment, roster.FieldNotes:
			values[i] = new(sql.NullString)
		case roster.FieldServiceDate, roster.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Roster fields.
func (_m *Roster) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case roster.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case roster.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case roster.FieldServiceDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field service_date", values[i])
			} else if value.Valid {
				_m.ServiceDate = value.Time
			}
		case roster.FieldDepartment:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field department", values[i])
			} else if value.Valid {
				_m.Department = value.String
			}
		case roster.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		case roster.FieldChurchID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field church_id", values[i])
			} else if value.Valid {
				_m.ChurchID = int(value.Int64)
			}
		case roster.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Roster.
// This includes values selected through modifiers, order, etc.
func (_m *Roster) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryChurch queries the "church" edge of the Roster entity.
func (_m *Roster) QueryChurch() *ChurchQuery {
	return NewRosterClient(_m.config).QueryChurch(_m)
}

// QueryEntries queries the "entries" edge of the Roster entity.
func (_m *Roster) QueryEntries() *RosterEntryQuery {
	return NewRosterClient(_m.config).QueryEntries(_m)
}

// Update returns a builder for updating this Roster.
// Note that you need to call Roster.Unwrap() before calling this method if this Roster
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Roster) Update() *RosterUpdateOne {
	return NewRosterClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Roster entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Roster) Unwrap() *Roster {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Roster is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Roster) String() string {
	var builder strings.Builder
	builder.WriteString("Roster(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("service_date=")
	builder.WriteString(_m.ServiceDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("department=")
	builder.WriteString(_m.Department)
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteString(", ")
	builder.WriteString("church_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ChurchID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Rosters is a parsable slice of Roster.
type Rosters []*Roster
